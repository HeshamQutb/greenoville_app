import 'package:flutter/material.dart';


class SoilTipsViewBody extends StatelessWidget {
  const SoilTipsViewBody({super.key});
  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [],
    );
  }
}
