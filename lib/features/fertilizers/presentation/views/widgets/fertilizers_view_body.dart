import 'package:flutter/material.dart';


class FertilizersViewBody extends StatelessWidget {
  const FertilizersViewBody({super.key});
  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [],
    );
  }
}
